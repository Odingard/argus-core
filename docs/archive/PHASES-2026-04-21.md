# ARGUS — Phase Plan (Spec-Reconciled, 2026-04-21) — ARCHIVED

> **Status — COMPLETE.** All six phases in this plan (0–5 plus the
> Fresh-Intel/OAuth-Supply-Chain pillar) shipped by 2026-04-24.
> ARGUS's current state (v0.5.0+: calibration, PRO consensus,
> diagnostic outer loop, reasoning audit, cross-run memory, etc.)
> is richer than this document describes. Preserved here for
> historical reference only — see the git log and [README](../../README.md)
> for the current architecture.

**Source of truth:** internal spec bundle
(Foundation, Agent_Specs, Tech_Architecture, Build_Roadmap, GTM_Playbook).

**Commitment — Option B:** ARGUS is an autonomous offensive platform that attacks **live AI deployments**. It is not a static source-code scanner. The pre-2026-04-21 codebase was the wrong product. This document is the rebuild plan to the spec.

**Non-negotiable per spec:**
> *"A finding is only confirmed when the injection causes a measurable, reproducible change in the agent's behavior — executing a command it should not execute, revealing information it should not reveal, or taking an action outside its intended scope. Observable behavior change is required. A model merely echoing the injection is not a finding."*

---

## Phase 0 — Foundation Complete (3 weeks)

Everything downstream requires this substrate. Without it, agents have nothing to attack.

### Tickets

| # | Ticket | Deliverable | Acceptance |
|---|---|---|---|
| 0.1 | Archive legacy | `legacy/` tree contains all static-scanner code with a README explaining the 2026-04-21 posture change. `src/argus/` contains only code that matches the spec. Tests still green or marked-skipped with reason. | `ls src/argus/` contains no static-scan path; `pytest tests/ -q` green (skipped tests OK with `archived` marker). |
| 0.2 | Target Adapter framework | `src/argus/adapter/`: `BaseAdapter`, `MCPAdapter` (promote internals from `mcp_live_attacker.py`), `HTTPAgentAdapter` (generic REST/chat API with pluggable auth), `StdioAdapter`, stub `A2AAdapter`. Each exposes `connect()`, `send(message)`, `observe()`, `disconnect()`. | `MCPAdapter(url).connect()` succeeds against `argus-targets/agent_09_mcp`; round-trips a benign message; emits structured observation. |
| 0.3 | Session State Manager | `src/argus/session/`: `Session` class tracks turn-by-turn conversation state, records transcripts, supports `.checkpoint()` / `.resume(id)` for multi-session attacks. | Unit tests: plant content in session A, disconnect, resume as session B, retrieve state. |
| 0.4 | Behavior Observation Engine | `src/argus/observation/`: deterministic baseline-vs-post-attack delta detection. Emits `BEHAVIOR_UNCHANGED` / `BEHAVIOR_DELTA(kind, evidence)` verdicts where `kind ∈ {UNAUTHORISED_TOOL_CALL, CONTENT_LEAK, STATE_MUTATION, IDENTITY_BYPASS, ...}`. | Unit tests on synthetic transcripts; no LLM in the validation path (spec calls this out — deterministic). |
| 0.5 | Attack Corpus v0.1 | `src/argus/corpus_attacks/`: versioned JSON/YAML directory of attack variants. Seed from the spec itself (200+ role hijacks, 500+ instruction overrides, encoded variants, tool-poisoning templates, memory-poisoning sequences, identity-spoof payloads). Entries carry provenance, severity, target surfaces. | ≥500 entries; `Corpus.sample(tag=...)` yields variants; pytest enforces no duplicate fingerprints. |
| 0.6 | Target Simulation Framework (labrat) | `src/argus/labrat/`: docker-compose generator that stands up a customer-equivalent target for safe attack. | `labrat up --config <yaml>` brings up an MCP + agent target; `labrat down` tears down; network-isolated from host. |
| 0.7 | Finding schema refactor | `AgentFinding` grows `evidence_kind` (behavior_delta / observation / corpus_variant_id), `baseline_ref`, `attack_variant_id`, `session_id`. | Existing pytests updated; all green. |
| 0.8 | **Dynamic Variant Generation** | `argus.corpus_attacks.dynamic`: (a) `LLMMutator` asks a model (via the routing layer) to produce N novel variants of any seed template; (b) `CrossoverMutator` combines two seed templates into hybrid variants; (c) `EvolveCorpus` ingests verified successful attacks back into the corpus as new seed templates with provenance. The 928 static variants are the bootstrap; this ticket commits the corpus to growing without bound. | Pytests with a stubbed LLM client verify (1) LLMMutator produces ≥3 distinct variants per template, (2) successful attack feedback creates a new template that round-trips through `Corpus`, (3) caching prevents duplicate LLM spend on the same template. |

### Process commitment — corpus is never frozen

The 928-variant static corpus from Ticket 0.5 + the LLM/crossover/feedback machinery from Ticket 0.8 mean ARGUS treats every engagement as corpus-growth. The rule, enforced by Phase-1+ agent code:

  > **Every validated finding becomes a new corpus template.** No exceptions. Provenance: `discovered_against:<target_id> on:<date>`. The next engagement starts with the previous one's lessons baked in.

This is the Raptor Cycle (Pillar 2) made concrete. Every scan deepens the moat for the scan after it.

### Phase 0 acceptance (single integration test)

The orchestrator connects `MCPAdapter` → `argus-targets/agent_09_mcp`, fires one `Corpus` variant (e.g. role-hijack #17), the Session Manager records the transcript, the Observation Engine emits a deterministic verdict. **No LLM in this loop.** Pure wiring proof that the Phase 0 substrate works end-to-end.

---

## Phase 1 — First Wave Live Agents (5 weeks)

Build the three agents your spec prioritises. Each runs *against the adapter*, not against source.

| Agent | What it does per spec | Phase 0 deps |
|---|---|---|
| **Agent 1 — Prompt Injection Hunter** | Fires direct + indirect + encoded + multi-step injections from Corpus against every input surface the Target Adapter exposes. Observation Engine confirms behaviour change. | 0.2 + 0.3 + 0.4 + 0.5 |
| **Agent 2 — Tool Poisoning Agent** | Registers adversarial MCP tool definitions (hidden unicode instructions, HTML-comment injection, param-description injection, cross-tool chaining, rug pull). Observes target consumption. | 0.2 + 0.4 + 0.5 |
| **Agent 9 — Supply Chain Agent** | Stands up rogue MCP server via labrat, version downgrade, update hijacking, dependency confusion. Observes target trust behaviour. | 0.2 + 0.4 + 0.6 |

**Phase 1 acceptance:** three-agent live swarm against a known-vulnerable MCP test target produces ≥1 validated behavior-change finding end-to-end. First customer-demo-able build.

---

## Phase 2 — Session & Multi-Turn Agents (5 weeks)

| Agent | Requires |
|---|---|
| **Agent 3 — Memory Poisoning** | 3-session protocol from spec: plant → retrieve → escalate. Needs 0.3 session manager at full power. |
| **Agent 4 — Identity Spoof** | A2A adapter (full impl — Google A2A, CrewAI handoff, LangGraph state edges). |
| **Agent 5 — Context Window** | Long-con multi-turn sustained-injection within one session. |
| **Correlation Agent v1 (runtime findings)** | Retarget existing correlator from static findings to runtime behavior-delta findings. |

---

## Phase 3 — Pipeline Attack Agents (5 weeks)

| Agent | Requires |
|---|---|
| **Agent 6 — Cross-Agent Exfiltration** | Multi-agent target in labrat; A2A adapter from Phase 2. |
| **Agent 7 — Privilege Escalation** | Live tool-call graph from observer; chain synthesis on runtime observations. |
| **Agent 8 — Race Condition** | Parallel-request primitive against shared-resource targets; TOCTOU detection. |

---

## Phase 4 — Completion + Integration (4 weeks)

| Deliverable |
|---|
| **Agent 10 — Model Extraction** — behavioural probing for system prompt / config / tool inventory. |
| **Correlation Agent v2** — full compound attack-path construction with runtime behavior-delta chains, CVE drafts, OWASP Agentic AI Top-10 mapping. |
| **CERBERUS rule generator** — every validated finding emits a CERBERUS detection rule. |
| **ALEC evidence-chain integration** — Wilson bundles flow into ALEC ingestion. |

---

## Pillar — Fresh-Intel / OAuth-Supply-Chain Coverage (added 2026-04-21)

Driven by April-2026 OAuth supply-chain intel (third-party AI-integration overgrant class) and the Claude Code v2.1.80 50-subcommand deny-rule bypass class. Extends the roster beyond the original 10-agent spec with a Phase-8 Environment Pivoting agent and the proof-grade evidence infrastructure a serious red-team platform needs:

| Deliverable |
|---|
| **Agent 11 — Environment Pivoting (EP-11)** — MAAC Phase 8 (Environment Pivoting). Ten techniques across four families: credential discovery, OAuth & grant surface, third-party integration audit, Workspace / PaaS pivot. Catches the third-party AI-integration OAuth-supply-chain class end-to-end. |
| **Command-Flooding mutator** — generic corpus mutator wrapping any payload in N filler subcommands joined by `;`, defaulting to 51 so a v2.1.80-shaped deny-rule scanner stops before reading the hostile tail. Seed templates in `seeds/command_flooding.json`. |
| **Deterministic Evidence (AI-Slop filter)** — `argus.evidence`: `EvidenceCollector`, `DeterministicEvidence` (pcap + container_logs + OOB callbacks), real `OOBListener` loopback HTTP server. `is_proof_grade()` gates findings so echo-only evidence doesn't ship as Wilson-Proof. |
| **SC-09 SC-T7 / SC-T8** — supply-chain catalog scanner adds oauth-overgrant (wildcard / "Allow All" scopes) and third-party-ai-integration (external AI-vendor hostnames in tool metadata). |
| **oauth_supply_chain corpus** — 7 seed templates (`osc_001..osc_007`) tagged `oauth_supply_chain_pattern`, probing workspace integration recon, token introspection, refresh-token audit, adjacent-SaaS pivot, env-secret scan, grant lifecycle. |
| **Phase 9 Impact Optimizer + BlastRadiusMap** — `argus.impact`: deterministic data classification (SECRET / PII / PCI / PHI / CREDENTIAL / BIOMETRIC) with Luhn-validated PAN matches and regulatory-tag unions (GDPR / HIPAA / PCI-DSS / GLBA / SOC2 / FedRAMP / CCPA / BIPA). `optimize_impact()` walks chain + evidence, applies default trust edges (AWS / GitHub / Slack / Stripe / PaaS / Workspace / JWT), and emits a harm-scored, regulator-defensible BlastRadiusMap. |

Pillar acceptance: `tests/test_pillar_oauth_supply_chain_acceptance.py` — real agents, real labrat, real evidence collected during the run. No manually constructed findings. Assertions on structural shape and union membership only.

---

## Phase 5 — Pilot + Corpus Growth (6 weeks)

Three pilots per GTM playbook. Every pilot finding → Corpus entry with provenance. The moat deepens with every engagement.

---

## What's kept (scaffolding for the real build)

| Kept | Why |
|---|---|
| `src/argus/swarm/` (blackboard + runtime) | Coordination layer correct |
| `src/argus/swarm/correlator.py` | Architecture correct; retarget inputs in Phase 2 |
| `src/argus/wilson/` (bundle + keys) | Evidence packaging target-agnostic |
| `src/argus/shared/ars.py`, `models.py`, `client.py`, `prompts.py`, `flywheel_reader.py` | Foundations still apply |
| `src/argus/routing/` | Per-job model router with failover |
| `src/argus/harness/` | Stateful harness — evolves into Phase 2's multi-turn substrate |
| `src/argus/audit/reasoning.py` | Applies to runtime findings too |
| `src/argus/personas/` | Applies to live agents same way |
| `src/argus/inventory/` | `argus --models` stays |
| `src/argus/drift/` | Applies to runtime findings |
| `src/argus/corpus/` (fine-tune corpus emitter) | Pillar 1; pairs with Corpus v0.1 (attack corpus) |
| `src/argus/mcp_attacker/` | Becomes core of MCPAdapter (Ticket 0.2) |
| `src/argus/integrations/` | Webhook, pre-commit, GitHub Action stay useful |
| `src/argus/layer0/fingerprinter.py` | Version detection applies to live targets too |
| `src/argus/layer6/cve_pipeline.py` | Advisory generation reusable |
| Pytest contract suite | Regression guard |

## What's archived to `legacy/`

| Archived | Reason |
|---|---|
| `layer1/` (static scanner) | Wrong target (source code) |
| `layer2/` (source trust graph) | Wrong target |
| `layer3/` (static fuzzer) | Wrong target |
| `layer4/` (static deviation detector) | Wrong target |
| `layer5/chain_synthesizer.py` | Built on static findings (wrong inputs) |
| `layer7/sandbox.py` (PoC exec on installed lib) | Wrong target shape |
| The 12 current `agents/*.py` (except `base.py`) | Source-scanners. Will be rebuilt against Target Adapters in Phases 1-4. |

Nothing is deleted. Everything recoverable from `legacy/`.

---

## Calendar (solo operator, full-time)

28 weeks total per the spec's own roadmap. This is a 6-month focused build. Part-time = ~1.5-2x that.

Ticket 0.1 begins now.
