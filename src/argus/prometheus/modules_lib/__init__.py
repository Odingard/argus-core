"""PROMETHEUS module library — auto-discovered by the registry.

Each subpackage corresponds to a top-level category:
  - injection/  (exploits)
  - payload/    (what runs after exploit)
  - auxiliary/  (recon)
  - post/       (post-exploitation)

Add a new module by dropping a Python file into the appropriate subpackage.
The registry will discover it on next load.
"""
