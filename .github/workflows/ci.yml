name: ARGUS CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ruff
        run: pip install ruff
      - name: Lint
        run: ruff check src/ tests/

  test:
    name: Contract + suite tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install package + test deps
        run: |
          pip install -e .
          pip install pytest pytest-asyncio
      - name: Run suite (offline)
        # Contract tests enforce the no-fabrication / MAAC-coverage /
        # real-library PoC invariants. All tests run offline —
        # argus-targets/ (third-party repos) is excluded.
        run: pytest tests/ -q --ignore=argus-targets
        env:
          ANTHROPIC_API_KEY: "dummy-key-for-offline-contract-tests"

  bandit:
    name: Bandit (SAST)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install bandit
        run: pip install bandit
      - name: Bandit high-severity scan
        # Medium + Low findings are allowed (mostly asserts in tests,
        # binds to 127.0.0.1 in dev tooling). CI fails on any High.
        run: bandit -r src/argus -q -lll

  semgrep:
    name: Semgrep (SAST)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install semgrep
        run: pip install semgrep
      - name: Semgrep auto-config
        # --error makes CI fail on any finding. Every flagged line in
        # the repo has a justified # nosemgrep: ... suppression.
        run: semgrep --config=auto --error --quiet src/argus/

  pip-audit:
    name: pip-audit (dependency CVEs)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install pip-audit
        run: pip install pip-audit
      - name: Install project
        run: pip install -e .
      - name: Audit
        # --skip-editable silences the cosmetic "argus-core (editable)
        # not on PyPI" warning. We intentionally drop --strict here —
        # --strict + --skip-editable conflict (strict fails on the
        # skipped editable). The run still fails on any real CVE in
        # the third-party dep tree.
        run: pip-audit --progress-spinner off --skip-editable

  detect-secrets:
    name: detect-secrets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install detect-secrets
        run: pip install detect-secrets
      - name: Audit against baseline
        # The baseline contains every labrat fake credential and
        # classifier regex pattern — real secrets would show up as
        # "new" findings and fail CI.
        run: |
          detect-secrets scan \
            --exclude-files '\.venv/|legacy/|\.claude/|argus-targets/' \
            --baseline .secrets.baseline

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test, bandit, semgrep]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install build
        run: pip install build
      - name: Build package
        run: python -m build
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: argus-dist
          path: dist/
